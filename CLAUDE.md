# Paperclip 仓库规则

## 迭代模式：快速迭代，不跑测试（用户 2026-08-27 定）

- 本仓库当前处于快速迭代期：**改完功能直接全量提交，不跑测试套件，不做提交前测试比对**。
- tsc 类型检查照常（编译错误挡运行），测试红绿不作为提交门槛。
- 声称「修好某个具体 bug」时仍然实测那一条链路（跑一次命令、curl 一次接口），但不为提交跑回归。
- 提交粒度：做完一件事就 `git commit`，不攒工作区。别的会话留在工作区的改动一并提交，注明来源即可。
